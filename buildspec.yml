phases:
  install:
    runtime-versions:
       docker: 18
  build:
    commands:
      - "echo Build started on `date`"
      - "echo Building the Docker image..."
      - "docker build -t cybage_ctg_devops_machineinfo_abhishek_cb ."	 
  post_build:
    commands:
      - "echo Build completed on `date`"
      - "echo Pushing the Docker image..."
      - "docker push abhishekmhadocker/cybage_ctg_devops_machineinfo_abhishek_cb:v1.1.2"
  pre_build:
    commands:
      - "echo Logging in to Docker Hub..."
      - "docker login --username=\"abhishekmhadocker\" --password=\"abhishek55@\""
version: 0.2
